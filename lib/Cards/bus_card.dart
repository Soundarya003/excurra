import 'package:flutter/material.dart';

class TrainCard extends StatelessWidget {
  const TrainCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
